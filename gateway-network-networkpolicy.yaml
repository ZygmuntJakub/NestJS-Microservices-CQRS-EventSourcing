apiVersion: extensions/v1beta1
kind: NetworkPolicy
metadata:
  creationTimestamp: null
  name: gateway-network
spec:
  ingress:
  - from:
    - podSelector:
        matchLabels:
          io.kompose.network/gateway-network: "true"
  podSelector:
    matchLabels:
      io.kompose.network/gateway-network: "true"
